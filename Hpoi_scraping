import requests
from bs4 import BeautifulSoup

URL = "https://www.hpoi.net"
page = requests.get(URL)

soup = BeautifulSoup(page.content, "html.parser")

results = soup.findAll("div", class_="hpoi-conter-ltsifrato")
print(results)